# Task index

This index is intentionally derived from the gate specifications rather than maintaining a second divergent backlog.

## Foundation and pure core

- MUI-00: build/module/CI/sanitizer/architecture foundation;
- MUI-01: renderer and framebuffer;
- MUI-02: interaction ownership;
- MUI-03: deterministic semantic mock world.

## Product baseline behavior

- MUI-04: 30-screen projection + stable inspectable element metadata;
- MUI-05: complete headless navigation/UX flows.

## Desktop laboratory

- MUI-06: C/SDL2 dark shell, scale 75–300%, backlight 0–1000%, virtual HAT, state/event panels, semantic inspector;
- MUI-07: named scenarios, faults, reproducible bug evidence/export.

## Freeze

- MUI-08: complete parity/cleanliness review and accepted baseline tag.

For implementation, use the checklist in the corresponding `gates/mui-XX-*.md`; do not mark this high-level index independently.
