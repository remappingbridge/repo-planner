# UX experiment workspace

This directory is reserved for process records created **after** the MUI-08 reconstruction baseline or for explicitly authorized blocking experiments.

Experiment records should capture hypothesis, branch/base, alternatives, observations, accepted/rejected result, specification impact, contract impact, and rollback point.

Disposable `lab/*` code does not become permanent architecture by default.
